/*const express = require("express");
const LoginController = require("../controllers/LoginController");
ESSA CONSTANTE FALA PARA IMPORTAR TUDO QUE ESTÁ NESSE CÓDIGO, OU SEJA, QUANO DUA O
LoginController.create, ELE ESTÁ FAZENDO O create ACESSAR ESSES CÓDIGOS.


const router = express.Router();

// Criar nova tarefa (sem validação)
router.post("/", LoginController.create);

// Buscar conta por email
router.post("/login", LoginController.compara);

module.exports = router;*/